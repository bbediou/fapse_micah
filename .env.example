# Contact form configuration (optional)
# Copy this file to .env and adjust values as needed.
# If .env does not exist, contact.php uses default values.

# Email recipient (where form submissions are sent)
MAIL_TO=benoit.bediou@unige.ch

# Email sender address and name (From header)
MAIL_FROM=no-reply@micah.unige.ch
MAIL_FROM_NAME=MICAH Website

# Note: contact.php uses PHP's mail() function.
# The mail() function relies on the server's MTA (mail transfer agent) configuration.
# Contact your hosting provider / IT if emails are not being sent or end up in spam.
